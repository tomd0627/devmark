# Anthropic API key — required for the Netlify Function
# Set this in the Netlify dashboard under Site configuration > Environment variables
# Never commit a real key to version control
ANTHROPIC_API_KEY=your_key_here
